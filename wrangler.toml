# 1. 对应报错中的 name mismatch，这里填报错里提到的名字 "bible"
name = "bible"

# 3. 对应报错的核心问题：必须指定兼容性日期
compatibility_date = "2026-01-24"

[assets]
directory = "./dist"
binding = "ASSETS"
not_found_handling = "single-page-application"
